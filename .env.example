# Copy to `.env` and fill in — NEVER commit .env to git.
# Values here are the lowest-priority layer; env vars and -D flags still win.

# base.url=https://staging.example.com

# Secrets are read only from env vars / -D flags via ConfigManager.secret(...).
# Example:
# ADMIN_PASSWORD=change-me
# API_TOKEN=sk-...
