# Sample environment overlay. Activated with -Denv=qa.
# Only put non-secret values here. Secrets belong in env vars or .env (gitignored).
base.url=https://playwright.dev
headless=true
timeout.ms=45000
